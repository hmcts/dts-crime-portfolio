# qa-regression-test-on-defect

Add QA acceptance criterion: every defect found anywhere — by QA exploration, by a user, in production, in a code review — SHALL produce a regression test in the same PR as the fix, so the same behaviour cannot return undetected.
