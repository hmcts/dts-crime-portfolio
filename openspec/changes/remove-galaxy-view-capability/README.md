# remove-galaxy-view-capability

Remove the `galaxy-view` capability and trim the two `galaxy_*` events from the
`analytics` closed event catalogue. Driven by the Product Manager's KEEP/KILL
pass — see [`decisions/2026-05-02-kill-galaxy-view.md`](../../../decisions/2026-05-02-kill-galaxy-view.md).
