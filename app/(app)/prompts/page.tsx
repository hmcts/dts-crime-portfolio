import { ComingSoonStub } from "@/components/shell/ComingSoonStub";

export default function PromptsPage() {
  return (
    <ComingSoonStub
      title="Prompts"
      spec="openspec/specs/prompts-library/spec.md"
      blurb="Community prompt library with tag and tool filters, sort modes, idempotent upvotes, comments, and a monthly competition leaderboard."
    />
  );
}
