import { ComingSoonStub } from "@/components/shell/ComingSoonStub";

export default function GalaxyPage() {
  return (
    <ComingSoonStub
      title="Galaxy view"
      spec="openspec/specs/galaxy-view/spec.md"
      blurb="Zoomable canvas constellation map of the portfolio with lens re-clustering, signal overlays, and dossier deep links."
    />
  );
}
