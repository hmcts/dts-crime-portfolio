import { ComingSoonStub } from "@/components/shell/ComingSoonStub";

export default function EventsPage() {
  return (
    <ComingSoonStub
      title="Events"
      spec="openspec/specs/events-listing/spec.md"
      blurb="Searchable list of events filterable by category and location, ordered by start time."
    />
  );
}
